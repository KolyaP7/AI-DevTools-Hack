[project]
name = "mcp_server"
version = "0.1.0"
description = "MCP server"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "python-dotenv",
    "opentelemetry-api",
]

[tool.hatch.build.targets.wheel]
packages = ["mcp"]
